# WebSocket URL (opcional)
# Se não definir, o frontend detectará automaticamente baseado no protocolo e host
# Útil para deployments em ambientes customizados
# Exemplo: NEXT_PUBLIC_WS_URL=wss://api.example.com/v1/ws/chat
# NEXT_PUBLIC_WS_URL=
